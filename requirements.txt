# 运行 app.py 推理所需的最小依赖（2026 年可用版本）
# 原版 requirements.txt 是 2021 年版本（mediapipe 0.8.4 / TF 2.9），已无法安装
mediapipe>=0.10.14
opencv-python>=4.8.0
numpy>=1.24.0

# 训练自定义手势（跑 notebook）需要以下两个；只跑推理可以不装
tensorflow>=2.16.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
